## Summary

- 

## Verification

- [ ] `npm run build`
- [ ] `npm test`

## Notes

- Compatibility/docs updated if this changes CLI, Terraform, template, or release behavior.

### Terraform changes (if applicable)
- [ ] Ran `terraform fmt -check -recursive`, `init -backend=false`, `validate`, and `terraform test` from `infra/terraform/`.
- [ ] Updated `docs/terraform/` rather than adding module docs under `infra/terraform/`.
