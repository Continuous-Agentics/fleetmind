# Skill manifest for the frontend-worker-bot template.
#
# Read by `fleetmind doctor` and `fleetmind sync-template frontend-worker-bot`.
# See openclaw/pm-bot/skills.yaml for the manifest format details.

role: frontend-worker

required:
  # Delegation protocol — worker side
  - name: bot-reception
    source: fleetmind

  # Universal bundled skills (every bot type gets these once built).
  # Commented out until each SKILL.md ships in openclaw/skills/.
  # - name: fleet-context
  #   source: fleetmind
  # - name: task-ledger-query
  #   source: fleetmind
  # - name: bot-self-update
  #   source: fleetmind
  # - name: agent-status
  #   source: fleetmind

  # Code-review competence — pulled from the ClawHub-published skill.
  - name: structured-pr-review
    source: clawhub
    author: ggettert
