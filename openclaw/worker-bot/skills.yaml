# Skill manifest for the worker-bot template.
#
# Read by `fleetmind doctor` and `fleetmind sync-template worker-bot`.
# See openclaw/pm-bot/skills.yaml for the manifest format details.

role: worker

required:
  # Delegation protocol — worker side
  - name: bot-reception
    source: fleetmind
  # Worker self-start protocol (CON-91)
  - name: worker-self-start
    source: fleetmind

  # Universal bundled skills (every bot type gets these once built).
  # Commented out until each SKILL.md ships in openclaw/skills/.
  # - name: fleet-context
  #   source: fleetmind
  # - name: task-ledger-query
  #   source: fleetmind
  # - name: bot-self-update
  #   source: fleetmind
  # - name: agent-status
  #   source: fleetmind
